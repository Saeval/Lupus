import React, { Component } from 'react';

class Roles extends Component {
    getRoles(){
        const roles = [
            "Popolano",
            "Lupo"
        ];
        return roles;
    }
}

export default Roles;